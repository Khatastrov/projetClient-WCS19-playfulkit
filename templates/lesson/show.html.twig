{% extends 'base.html.twig' %}

{% block title %}{{ lesson.title }}{% endblock %}

{% block body %}

    <aside class="float-lg-right mr-2">
        <strong>Les articles les plus récents :</strong>
        <ul>
            {% for recent in latest %}
                <li>
                    <a href="{{ path('lesson_show', {'id': lesson.id}) }}">{{ recent.title }}</a>
                </li>
            {% endfor %}
        </ul>
    </aside>

    <small>
        {% if lesson.author  %}
            Publé par {{ lesson.author.username }}
        {% endif %}
        {% if lesson.publicationDate %}
            le {{ lesson.publicationDate|date('d-m-Y') }}
        {% endif %}
    </small>
    <div class="container">
        <div class="row">
            <h1 class="mx-auto">{{ lesson.title }}</h1>
        </div>
        <article class="row single-post mt-5 no-gutters">
            <div class="col-md-12">
                {% if lesson.illustration %}
                    <div class="image-wrapper float-left pr-3">
                        <img src="{{ lesson.illustration }}" alt="illustration de la leçon">
                    </div>
                {% endif %}
                <div class="single-post-content-wrapper p-3">
                    {{ lesson.content }}
                </div>
            </div>
        </article>
        <div class="d-flex justify-content-center">
            <a href="{{ path('lesson_index') }}" class="btn btn-info mx-auto">Retour à la liste des leçons &rarr;</a>
        </div>
    </div>

{% endblock %}