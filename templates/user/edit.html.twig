{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_password_edit', {'id' : app.user.id }) }}">Pour modifier ton mot de passe, c'est par ici !</a>
    <a href="{{ path('user_show', {'id': app.user.id}) }}">Annuler</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
