{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.username, {'label': 'Ton pseudo :'}) }}
    {{ form_row(form.email, {'label': 'Ton email :'}) }}
    {{ form_row(form.lastname, {'label': 'Ton nom de famille :'}) }}
    {{ form_row(form.firstname, {'label': 'Ton prénom :'}) }}
    {{ form_row(form.address, {'label': 'Ton adresse :'}) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
