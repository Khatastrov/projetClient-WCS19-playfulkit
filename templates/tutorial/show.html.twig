{% extends 'base.html.twig' %}

{% block title %}{{ tuto.title }}{% endblock %}

{% block body %}
    <article>
        <h2>{{ tuto.title }}</h2>
        <iframe width="560" height="315" src="{{ tuto.illustration | replace({ 'watch?v=' : 'embed/' }) }}"></iframe>
        <div class="metadata">Ecrit le {{ tuto.dateCreation | date('d/m/Y') }} à {{ tuto.dateCreation | date('H:i') }} </div>
        <div class="content">
            {{ tuto.content | raw }}
        </div>
    </article>
{% endblock %}

